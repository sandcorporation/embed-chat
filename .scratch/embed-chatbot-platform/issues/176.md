# 176 — 랜딩 페이지 라이브 at `/` (엔트리·서빙·소개·Contact)

Status: done
Labels: done

## Parent

[PRD-landing-page.md](../PRD-landing-page.md)

## What to build

`/`에 공개 랜딩 페이지를 띄운다(정적, 백엔드 없음). admin 레포에 멀티페이지 landing 엔트리를 추가하고
nginx가 루트에서 서빙. 이 슬라이스는 페이지 골격 + 소개/특징 + 연락처까지(데모는 후속 슬라이스).

- admin 멀티페이지 빌드에 landing 엔트리 추가, admin SPA는 `/admin-ui/` 그대로(무변경).
- nginx에 `location /` 추가해 landing을 루트에서 정적 서빙(자산 base 처리 포함).
- 섹션: Hero(카피 + "운영자 로그인"→`/admin-ui/` CTA) · 소개/특징 카드 4개(지식그래프 RAG·실시간
  토큰 스트리밍·HITL 상담 전환·임베드 위젯) · Contact(Email `gksdjf1690@gmail.com` mailto · 전화
  `010-2483-1690` tel) · footer.
- 스타일: admin Tailwind + shadcn 재사용, 다크모드, 한국어. 브랜드 "Embed Chat".

## Acceptance criteria

- [ ] `/`가 랜딩 HTML을 200으로 서빙(admin은 `/admin-ui/` 그대로)
- [ ] 소개·특징 카드 4개 렌더, 로그인 CTA가 `/admin-ui/`를 가리킴
- [ ] Contact에 `mailto:gksdjf1690@gmail.com`·`tel:01024831690` 링크 존재
- [ ] admin vitest 회귀 GREEN + 랜딩 렌더 테스트 GREEN (Docker)

## Blocked by

- None - can start immediately
