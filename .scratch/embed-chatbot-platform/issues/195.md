# 195 — uvicorn ASGI + async view + SSE async generator

Status: done
Labels: ready-for-agent

## Parent

[PRD-full-async-rewrite.md](../PRD-full-async-rewrite.md)

## What to build

- gunicorn gevent → uvicorn(ASGI), Django ASGI app.
- ninja chat/SSE view async화. SSE를 ASGI async generator(async redis 구독)로.
- ORM aget/asave(스레드풀) + executor 튜닝. 옛 gevent 경로 제거.

## Acceptance criteria

- [x] uvicorn으로 web 기동, async view 동작 (config/asgi.py, stream/send_message async)
- [x] SSE async generator로 토큰 스트리밍(async redis 구독) — asse_event_stream
- [x] 전체 테스트 그린(async client) — 392 passed
- [x] chat Celery 경로 완전 제거(taskiq 단일화) — tasks.py·sync 래퍼·큐 라우팅 삭제

## Blocked by

- 194
