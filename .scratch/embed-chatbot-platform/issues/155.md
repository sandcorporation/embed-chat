# 155 — checkpoint 비대화: 턴 한정 검색 산출물(rag_chunks) 휴지 상태에서 비우기

Status: done
Labels: done

## Parent

운영 버그 리포트(no PRD). 관련: [PRD-rag-query-checkpoint-hitl-realtime.md](../PRD-rag-query-checkpoint-hitl-realtime.md) (checkpoint 뷰어)

## What to build

어드민 Checkpoint 뷰어(`GET /sessions/{id}/checkpoint`)가 "지식그래프 외의 데이터가 엄청나게
많이" 보이는 문제. 실측 결과 `rag_chunks` 채널이 단일 세션 66KB(단일 버전 20KB)로 `messages`
(6.5KB)의 10배. `source_search`가 보강하는 원문 TextUnit 청크가 그 정체 — 턴마다 새로 조립되는
임시 산출물인데 그래프 채널이라 super-step 스냅샷마다 영구 저장된다. 종단 노드에서 비워
체크포인트엔 대화(messages)만 남긴다.

## Acceptance criteria

- [x] 원문 폴백으로 rag_chunks가 채워진 뒤에도 휴지 체크포인트의 `rag_chunks == []`
- [x] `visitor_memories`·`operational_notice`도 휴지 상태에서 비움
- [x] 대화(messages)는 보존
- [x] 회귀 테스트 GREEN, 백엔드 풀스택 GREEN

## Resolution

- 커밋 `417c64d` — `nodes.py` 종단 노드(save_messages·create_escalation)에서 `_clear_transient()`.
- 원칙: 임시 산출물을 LangGraph 그래프 채널에 두지 말 것(메모리 노트 `checkpoint-channel-visibility`).

## Blocked by

- None
