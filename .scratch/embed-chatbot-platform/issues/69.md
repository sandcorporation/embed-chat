# Issue 69: DocumentChunk/retriever/pgvector 제거 + 벡터 테스트 마이그레이션

Status: ready-for-agent
Labels: ready-for-agent

## Parent

PRD: `.scratch/embed-chatbot-platform/PRD-pgvector-retirement.md`
ADR: `docs/adr/0007-graphrag-neo4j-replaces-2step-vector-rag.md`

## What to build

쓰이지 않게 된 벡터 RAG 코드를 제거해 그래프를 단일 진실로 만든다.

- `DocumentChunk` 모델 + `document_chunks` 테이블 제거(마이그레이션).
- 옛 `retriever`(retrieve_chunks/retrieve_chunks_with_scores) 제거.
- 재임베딩 잔여 코드 제거.
- RAG 용도의 pgvector 의존성/`VectorField` 사용 제거(임베딩은 Neo4j 벡터 인덱스). DB 이미지는 유지 가능하나 RAG 미사용.
- 벡터 시대 테스트(`tests/test_rag.py`의 청크 생성/조회·retriever·재임베딩·Label prefix 검색)를 그래프 기준으로 재작성하거나 폐기.

## Acceptance criteria

- [ ] DocumentChunk 모델/테이블/retriever/재임베딩 코드가 제거된다
- [ ] RAG 경로에서 pgvector(VectorField)가 더 이상 쓰이지 않는다
- [ ] 전체 백엔드 스위트가 그래프 기준으로 통과한다(폐기 테스트 정리 포함)
- [ ] 전체 E2E 스위트가 통과한다
- [ ] chat/검색/청크 인스펙터/레이블 동작이 회귀 없이 유지된다

## Blocked by

- Issue 68
