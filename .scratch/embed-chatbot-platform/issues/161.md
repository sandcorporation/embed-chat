# 161 — prod에서 Paddle 제거 + 설정 + ADR-0009 개정/신규 ADR + README

Status: done
Labels: done

## Parent

[PRD-vision-ocr.md](../PRD-vision-ocr.md)

## What to build

prod에서 Paddle GPU 의존을 제거하고(dev/test 유지) 문서를 정리한다.

- prod compose에서 `paddle-ocr` 서비스 제거. dev·test compose는 유지. `PADDLE_OCR_URL`/`PADDLE_OCR_TIMEOUT`은 dev/test 전용. prod 기본 OCR 엔진=vision.
- ADR-0009 개정(보충): 금지 범위를 "*깨진 텍스트의 LLM 정제*"로 한정, "*픽셀로부터의 vision OCR(전사 가드레일 하)*" 허용 명시.
- 새 ADR: vision OCR 채택(per-Tenant provider, Paddle dev/test 격하) 결정·trade-off 기록.
- README: OCR 섹션을 Paddle 고정 → per-Tenant Vision Provider(dev/test=Paddle 폴백)로 갱신.

## Acceptance criteria

- [ ] prod compose에 paddle-ocr 없음, dev/test엔 있음
- [ ] prod에서 OCR 미설정 시 명확한 에러(GPU 미기동에도 부팅 정상)
- [ ] ADR-0009 개정 + 새 ADR 추가
- [ ] README OCR 설명 갱신
- [ ] 백엔드 풀스택 GREEN

## Blocked by

- 158
