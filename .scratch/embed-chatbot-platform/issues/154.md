# 154 — 이벤트 파이프라인 관측: relay·소비자 INFO 로깅 + LOGGING 설정

Status: done
Labels: done

## Parent

[PRD-eventdriven-hitl-outbox.md](../PRD-eventdriven-hitl-outbox.md)

## What to build

bridge/relay 컨테이너의 `docker logs`에서 어떤 이벤트가 오갔는지 보이도록, 처리/멱등스킵/발행을
INFO로 남긴다. Django 기본 로깅은 WARNING+만 내보내므로 `apps.events` 로거를 stdout INFO로
설정한다.

## Acceptance criteria

- [x] 소비자가 처리한 이벤트마다 `[event] group=… type=… event_id=… aggregate=… -> handled` INFO 로그
- [x] 멱등 스킵 시 `-> skipped(duplicate)`, relay 발행 시 `[relay] published …` 로그
- [x] `settings`에 `apps.events` → stdout INFO `LOGGING` (이중 출력 없음)
- [x] caplog 기반 테스트 GREEN, 백엔드 풀스택 GREEN, 라이브 컨테이너에서 출력 확인

## Resolution

- 커밋 `b279ac6` — `consumer.py`/`relay.py` INFO 로깅 + `config/settings/base.py` `LOGGING`.
- 운영 문서: `docs/event-pipeline.md` 관측 섹션.

## Blocked by

- None
