# 202 — 캐싱 통합 테스트 플로우 확장

Status: done
Labels: ready-for-agent

## Parent

[PRD-prompt-caching-graph-flow-test.md](../PRD-prompt-caching-graph-flow-test.md)

## What to build

201의 캡처 하네스로 나머지 그래프 플로우에서도 안정 prefix 불변식을 검증한다.

- 원문 폴백(context_sufficient=False → source_search → 2번째 call_llm, rag_chunks 증가)에서 두 call의
  system prefix byte-동일(폴백이 휘발성을 system에 안 섞음).
- 영업시간 외(operational_notice 존재): notice는 trailing 턴에만, system prefix 불변.
- 주제범위 ON: `scope_instruction`(테넌트 불변)이 system에 들어가도 그 테넌트의 모든 턴에서 prefix
  byte-동일(캐싱 안 깨짐).

## Acceptance criteria

- [x] 폴백 2회 호출의 system prefix byte-동일, RAG는 trailing에만
- [x] 영업시간 외 operational_notice는 trailing에만, system 불변
- [x] 주제범위 ON 테넌트에서 모든 턴의 system prefix byte-동일
- [x] 전체 테스트 그린

## Blocked by

- 201
