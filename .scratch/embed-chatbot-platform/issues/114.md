# 114 — ProviderModels 딥모듈 (모델 조회 + 기능호출 검증)

Status: ready-for-agent

## Parent

[PRD-provider-model-discovery.md](../PRD-provider-model-discovery.md) · [ADR-0012](../../../docs/adr/0012-per-tenant-llm-embedding-providers.md)

## What to build

provider API를 호출해 정규화된 모델 목록을 주고, provider 연결을 기능 호출로 검증하는 deep module. provider HTTP는 외부 경계이므로 결정적 Fake로 교체해 단독 검증한다.

- `list_provider_models(type, base_url, api_key) -> list[str]`: 타입별 분기
  - OpenAI-호환(openai/custom/"" 플랫폼기본): `GET {base_url}/models` → `data[].id`
  - Anthropic: `GET /v1/models`(x-api-key + anthropic-version 헤더) → `data[].id`
  - ollama(플랫폼기본 임베딩): `GET {ollama}/api/tags` → `models[].name`
  - 실패 시 `ProviderError`(사람이 읽는 메시지), httpx 타임아웃
- `validate_provider(kind, type, base_url, api_key, model) -> None`: 기능 호출 검증
  - kind=llm → `list_provider_models` 성공(연결+키)
  - kind=embed → 1-텍스트 임베딩 호출 성공(provider 실제 용도)
  - 실패 시 `ProviderError`. 연결성만 검증(특정 model 상장 여부는 강제 안 함)

## Acceptance criteria

- [ ] openai/custom 타입: `/models`의 `data[].id` 목록 반환
- [ ] anthropic 타입: Anthropic models 엔드포인트에서 id 목록 반환(헤더 포함)
- [ ] ollama(플랫폼기본 임베딩): `/api/tags`의 `models[].name` 반환
- [ ] 조회 실패(연결/4xx) 시 `ProviderError`
- [ ] `validate_provider(llm)`은 목록 조회 성공으로, `validate_provider(embed)`은 1-텍스트 임베딩 호출 성공으로 통과; 실패 시 `ProviderError`
- [ ] provider HTTP를 Fake로 교체한 pytest(타입별 정규화·에러·검증)

## Blocked by

None - can start immediately
