# 194 — taskiq chat_task + dispatch + 워커

Status: done
Labels: done

## Parent

[PRD-full-async-rewrite.md](../PRD-full-async-rewrite.md)

## What to build

- chat_task(taskiq async): run_chat_agent_async + 세션 락 + timeout + 예외 시 publish_error.
- dispatch_chat 어댑터: chat_task.kiq(...). 뷰가 호출(threading/celery 경로 제거).
- taskiq 워커 서비스. Celery worker-chat 폐기.
- taskiq 설정: max-async-tasks(백프레셔)·timeout·재시도 끔(at-most-once)·graceful.

## Acceptance criteria

- [ ] POST /chat/message(non-hitl) → InMemoryBroker 인라인 → assistant 저장
- [ ] 세션 락·timeout·publish_error 동작
- [ ] worker-chat(Celery) 제거 → taskiq 워커 대체

## Blocked by

- 192, 193
