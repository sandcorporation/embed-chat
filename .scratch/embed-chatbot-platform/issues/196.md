# 196 — 배포 전환 + 무중단 검증

Status: in-review (코드/compose 완료, prod 무중단 검증만 HITL 잔여)
Labels: ready-for-agent, HITL

## Parent

[PRD-full-async-rewrite.md](../PRD-full-async-rewrite.md)

## What to build

- compose(prod.oracle): api command uvicorn, taskiq 워커 서비스, gevent/celery worker-chat 제거.
- docker rollout 대상 갱신. 무중단 스모크.

## Acceptance criteria

- [x] compose가 uvicorn·taskiq로 전환 (base/dev/prod.oracle/test, e2e는 chat-worker-e2e 분리)
- [ ] docker rollout 무중단(start-first) 검증 — **prod 수동(HITL)**
- [ ] 배포 스모크(/api/health + chat 1턴) — **prod 수동(HITL)**

추가 완료:
- [x] config/asgi.py(uvicorn) + config/taskiq_worker.py(Django setup + 태스크 등록)
- [x] 장수 워커 Django 커넥션 위생(close_old_connections 미들웨어, ORM 스레드에서 호출)
- [x] gunicorn·gevent 의존성 제거
- [x] 배포 문서(deployment.md 최초 async 컷오버 절차) + README/architecture 갱신

## Blocked by

- 195

## Notes

HITL — prod 배포·무중단 검증 수동. 코드·compose·문서는 완료. 컷오버 절차는
docs/deployment.md "최초 async 컷오버(1회)" 참조(유휴 시각 권장, chat은 at-most-once라 재전송 허용).
